class Fish {
  // Declare fields
  float x;
  float y;
  float speed;

  // Default Constructor
  Fish() {
    this.x = 10;
    this.y = 500;
    this.speed = 2;
  }

  // Constructor
  Fish(float x, float y, float speed) {
    this.x = x;
    this.y = y;
    this.speed = speed;
  }
}
